import { Typography } from '@material-ui/core';
import React from 'react';
import Layout from '../src/Components/Layout';

const more = () => {
    return (
        <Layout>
            <Typography variant="h6" color="secondary">This is more page dummy content.</Typography>
        </Layout>
    );
};

export default more;