import { Typography } from '@material-ui/core';
import React from 'react';
import Layout from '../src/Components/Layout';

const data = () => {
    return (
        <Layout>
            <Typography variant="h6" color="secondary">This is data page dummy content.</Typography>
        </Layout>
    );
};

export default data;